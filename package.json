{
  "name": "live-api-spec",
  "version": "1.0.0",
  "main": "index.ts",
  "private": true,
  "author": {
    "name": "Martynas Petuška",
    "email": "martynas.petuska@gmail.com"
  },
  "repository": "git@bitbucket.org:cambridgeassessment/ee-api-spec.git",
  "scripts": {
    "clean": "rimraf dist coverage src/**/*.js src/**/*.js.map test/**/*.js test/**/*.js.map",
    "start:dev": "nodemon src/index.ts",
    "build:app": "tsc && cpy raml !raml/index.html ../dist --cwd=src --parents",
    "build:console": "ts-node scripts/buildConsoles.ts",
    "build": "yarn build:console && yarn build:app",
    "build:start": "yarn build && yarn start",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "osprey-mock-service": "^0.4.4"
  },
  "devDependencies": {
    "@types/cors": "^2.8.6",
    "@types/express": "^4.17.4",
    "@types/node": "^13.11.0",
    "api-console-cli": "^2.0.3",
    "cpy-cli": "^3.1.0",
    "env-cmd": "^10.1.0",
    "nodemon": "^2.0.2",
    "replace": "^1.1.5",
    "rimraf": "^3.0.2",
    "ts-node": "^8.8.2",
    "typescript": "^3.8.3"
  }
}
